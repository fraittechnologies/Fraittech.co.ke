# Copy this file to `.env` next to config.php. The real `.env` is gitignored.
# SMTP password for info@fraittech.co.ke
#
# If the password has spaces, #, or =, wrap it in double quotes:
# FRAITTECH_SMTP_PASSWORD="your_password_here"

FRAITTECH_SMTP_PASSWORD=your_mailbox_password_here
